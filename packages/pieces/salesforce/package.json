{
  "name": "@nexiom/piece-salesforce",
  "version": "0.5.1",
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.lib.json",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "devDependencies": {
    "vitest": "^4.0.18"
  },
  "dependencies": {
    "@nexiom/connectors": "workspace:*",
    "dayjs": "^1.11.19"
  }
}